# Contributing

感谢你改进 Brain Neuroimage Processing。这个仓库由多个相对独立的研究模块组成，因此贡献应尽量小而清晰。

## 提交前

1. 在 Issue 中说明问题、所用模块、输入数据格式和可复现步骤。
2. 不要提交真实研究数据、受试/动物标识、上传文件、模型权重、日志或批量结果图。
3. 不要提交虚拟环境、`node_modules`、IDE 配置或上游项目的完整源码副本。
4. 新增行为或指标时，请同时说明科学依据、参数默认值和失败边界。

## 代码约定

- Python 代码应保持可导入，并为新增的纯函数或安全边界添加测试。
- 优先通过函数参数或命令行参数接收输入、输出路径，避免加入新的机器绝对路径。
- 随机流程应暴露或固定种子；结果文档不得把一次实验输出写成普遍性能结论。
- Web API 仅按本地研究工具设计。涉及外网部署的改动必须同时考虑认证、文件隔离、限流与日志脱敏。

## 最小验证

```bash
cd ai-mouse/BN/calcium_analysis_platform/backend
python -m unittest discover -s tests
```

对其他模块，请在 Pull Request 中写明实际运行的脚本、输入是否为合成数据，以及未能运行的检查。
